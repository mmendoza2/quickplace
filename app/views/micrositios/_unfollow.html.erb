<%= form_for(current_user.relationmicrositios.find_by(followed_id: @micrositio), html: { method: :delete }, remote: true) do |f| %>
  <%= f.submit "Favorite place", class: "btn btn-large" %>
<% end %>